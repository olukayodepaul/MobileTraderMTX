object AndroidX {
    private const val coreKtxVersion = "1.8.0"
    const val coreKtx = "androidx.core:core-ktx:$coreKtxVersion"

    private const val appCompatVersion = "1.4.0"
    const val appCompat = "androidx.appcompat:appcompat:$appCompatVersion"

    private const val appconstraintlayout = "1.4.0"
    const val appConstrain = "androidx.constraintlayout:constraintlayout:$appconstraintlayout"

    private const val lifecycle_version = "2.2.0-alpha03"
    const val lifecycle_common = "androidx.lifecycle:lifecycle-common-java8:${lifecycle_version}"
    const val lifecycle_liveData = "androidx.lifecycle:lifecycle-livedata-ktx:${lifecycle_version}"
    const val lifecycleViewModel = "androidx.lifecycle:lifecycle-viewmodel-ktx:${lifecycle_version}"
    const val lifecycle_runtime = "androidx.lifecycle:lifecycle-runtime:${lifecycle_version}"

    private const val life_cycle_extension_version = "2.2.0-alpha03"
    const val life_cycle_extension = "androidx.lifecycle:lifecycle-extensions:${life_cycle_extension_version}"

    private const val recycle_view_version = "1.1.0-beta03"
    const val recyclerView = "androidx.recyclerview:recyclerview:${recycle_view_version}"
}